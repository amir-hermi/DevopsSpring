version: "3.8"
services:
  mysqldb:
    image: mysql
    restart: unless-stopped
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_DATABASE=devops
    ports:
      - 3306:3306
  app:
    image: noor99/devops_project:2.1
    depends_on:
      - mysqldb
    restart: on-failure
    ports:
      - 8082:8082
    stdin_open: true
    tty: true